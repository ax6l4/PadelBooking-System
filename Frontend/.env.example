# Copy to .env.development or .env.production and adjust for your environment

# Development (optional — Vite proxy uses /api by default)
# VITE_API_URL=http://localhost:5104/api

# Production demo build (required when using npm run build + preview)
VITE_API_URL=http://localhost:5104/api
